using Dalamud.Interface;
using Ocelot.Config.Attributes;

namespace CrescentIsleUsefulTool;

internal sealed class AutomationModeCompatibleAttribute() : IconAttribute(FontAwesomeIcon.Robot, "generic.tooltip.automation_mode_compatible", 0.25f, 0.65f, 1.0f);
