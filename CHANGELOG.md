# 4.7.2

- 自動操作モード、通常トレジャーハンター、マジックポット宝箱探索が同時に移動命令を出さない排他制御を追加し、FATE／CE完了時は通常トレジャーハンターを一時停止
- マジックポットの財宝誘導中は必須帰還要求を保持したまま帰還チェーンを停止し、宝箱探索完了後にデミデジョン帰還を自動再開
- FATE／CEへの移動停止を20秒で検知して最大2回経路を再作成し、3回目は対象を一時除外して拠点帰還へ切り替える監視処理を追加
- 必須帰還に35秒の進捗監視を追加し、チェーン中断・移動停止時にデミデジョン済み状態を保持しながら安全に再試行
- 戦闘状態だけが残った場合はその場で待たず最寄りの魔導通路方向へ退避し、移動停止時は経路を再作成
- トレジャーハンター開始時に古い移動判定時刻を初期化し、開始前のバフ・帰還待ちを移動停止と誤判定して最初の座標をスキップする問題を修正
- マジックポット宝箱探索にも12秒の停止・35秒の目的地進捗監視を追加し、停止した経路を自動再作成
- 「経路検索設定」の最大エリアレベルを47まで拡張し、宝箱・にんじん探索では実際のvnavmesh経路上に上限超過の敵を検知すると接近しない安全判定を追加
- 廃止済みテレポーター設定の保存項目を削除し、FATE／CE完了後の帰還・エーテライト接近を単一の必須帰還コントローラーへ統一
- 状態マネージャーの次の5ステップ先頭を、説明用固定文ではなく実際に実行中の帰還ステータスへ変更

# 4.7.1

- 自動操作モードとトレジャーハンター開始時の「たんきゅうしん」を、デミデジョンで拠点へ戻りナレッジクリスタル付近まで移動・停止してから実行するよう変更
- ナレッジクリスタルの近接確認前に、すっぴんへの変更や「たんきゅうしん」のアクション要求を行わない安全条件を追加
- 「現在の設定」の値列を可変幅と折り返し表示へ変更し、狭い画面では設定名と現在値を縦積みにして戦闘AI名・優先順位などの見切れを修正

# 4.7.0

- 北征編で追加された8種類のサポートジョブIDとステータスIDを追加し、現在ジョブを誤って「すっぴん」と判定していた問題を修正
- バフが未付与またはしきい値以下の場合、すっぴんへ切り替えて「たんきゅうしん」を実行し、北征編ジョブを含む実行前のサポートジョブへ確実に復帰するよう変更
- CE終了後も戦闘状態が残ると状態マネージャーがCE参加中から遷移しない不具合を修正
- FATE／CE完了時の帰還処理を一本化し、必ずデミデジョンを実行して拠点エーテライトへ移動、重複通知・中断・失敗時は二重詠唱を防ぎながら再試行
- テレポーター設定を廃止し、最寄りエーテライト経由、長距離時のマウント、完了後の帰還とエーテライト接近を固定動作へ変更
- 通貨・経験値計測の設定を廃止し、計測機能を常時有効化
- メイン画面に「優先順位」タブを追加し、マジックポット／CE／FATEの選択順を任意に並べ替え可能に変更
- 状態マネージャーへ、現在の状況に応じた次の自動操作5ステップを常時表示
- マジックポット次回予想が5分未満になった場合、FATE／CE参加中を除いて進行中の移動を中断し、発生待機へ移行する処理を追加
- 北征編トレジャーハンターはベースキャンプ起点の固定巡回順を維持し、進行不能を検知した場合に限って残り経路を再作成、同一地点で再失敗した場合のみスキップする復旧処理を追加
- 経路計算待ちを8秒、移動停止判定を12秒へ短縮し、目的地へ近づかない状態も35秒で再経路生成へ移行
- vnavmeshの経路計算中に次のIPC要求を連打して停止していた問題と、宝箱接近時に操作チェーンを重複登録していた問題を修正
- デミデジョンを騎乗・戦闘・移動・詠唱状態の解消後に再試行し、エリア移動完了まで確認する方式へ変更
- 4.6.0ログで大量発生していた翻訳警告と短時間チェーン破棄ログを抑制し、経路・帰還待ちの通常タイムアウトを静音化

# 4.6.0

- メイン画面に「現在の設定」タブを追加し、自動操作、移動、宝箱、FATE／CE監視、通知、補助機能、バフ、ウィンドウ設定の保存値を日本語で一覧表示
- マギ・トレジャーサーチのメッセージ解析を修正し、開始時に判明した残り青銅・白銀宝箱数を個別表示
- 北征編トレジャーハンターを毎回同じ固定順で地上の全座標へ接近する経路へ変更し、宝箱を検出できない地点では次の座標へ継続
- マジックポット宝箱を通常宝箱と分離して管理し、到着後にマウントを降り、1回だけ操作して開封詠唱と消滅確認を安全に待つ処理を追加
- FATE／CEが移動中に消滅した場合も完了として扱い、アクティビティがない地点で停止せずデミデジョンと拠点エーテライトへの移動を再実行
- 「たんきゅうしん」の対象バフを1秒ごとにしきい値確認し、安全に実行できる状態になった時点で再付与する処理を追加
- 自動操作設定を「基本設定」「南征編」「北征編」の3タブへ再設計し、モード本体の開始・停止をメイン画面だけに集約
- メイン・設定・自動操作ウィンドウのタイトルにバージョンを表示し、主要な英語状態表示を日本語化
- テレポーター設定の初期値をすべて有効へ統一し、カード説明の折り返しと宝箱画面の横幅不足を修正

# 4.5.0

- 自動操作モードまたはトレジャーハンターの開始時に、すっぴんへ一時変更して「たんきゅうしん」（Action ID 46606）を必ず使用し、実行前のサポートジョブへ復帰する処理を追加
- バフ設定の誤表記「探究心」を「たんきゅうしん」へ修正し、アクションIDと通常更新・開始時必須実行の違いが分かる専用設定画面へ刷新
- 北征編の宝箱経路を魔導通路の担当エリア単位で分割し、別エリア間では徒歩直行せず必ず魔導通路を経由するよう変更
- 各移動前のvnavmesh経路検証、空経路・計算失敗・移動停止の検知、到達不能座標の自動除外と件数表示を追加
- メイン画面を「概要」「FATE・CE」「宝箱・探索」「支援ツール」「計測」の5ページに整理し、小さい画面では1カラムへ切り替わるコンパクトUIへ刷新
- 設定画面をCIUTテーマのカテゴリナビゲーション、設定カード、北征編コンテンツタブへ再設計

# 4.4.0

- 宝箱への操作送信後に消滅を待っていた最大10秒の待機を廃止し、開封後すぐ次の巡回地点へ進むよう変更
- 北征編南西高台の上下2組の乱気流を内部レイアウトから特定し、乱気流で高台へ入り、対象座標の巡回後に反対側の乱気流から出る専用経路を追加
- トレジャーハンター開始・再開時に「マギ・トレジャーサーチ」を必ず使用し、開始時の残り宝箱数を再取得するよう変更
- メイン画面に開始時残数、地上での取得数、地下空洞を除く全座標巡回後の残数照合結果を追加
- 未出現判定を設定上の検知距離ではなく座標の6m以内で行い、地上の全座標へ確実に接近して確認するよう修正

# 4.3.0

- 北征編トレジャーハンターの開始・再開時に必ずデミデジョンを実行し、北部ベースキャンプから巡回を開始するよう変更
- 宝箱の種別IDではなく内部レイアウトの座標IDを使用し、地下空洞を除く地上59座標をすべて一度ずつ探索するよう修正
- 開封・確認済みの座標を記録し、停止後の再開時にも同じ座標を再確認しないよう変更
- メイン画面のCE名を日本語で表示するよう修正
- メイン画面にCE・FATE自動操作の開始・終了ボタンを追加

# 4.2.1

- トレジャーハンターで青銅・白銀の宝箱を開封するとき、マウントから降りずに操作するよう変更

# 4.2.0

- マジックポット宝箱到着時のクラッシュ原因だった、終了済みFATEのゲーム内ポインター保持を廃止
- FATE、CE、宝箱、にんじん、敵オブジェクトを安全な管理データへ変換し、操作直前にゲーム内オブジェクトを再取得する方式へ変更
- vnavmesh／Lifestreamが未起動・再読込中でも、自動操作の停止や移動処理からIPC例外を出さないよう保護
- ゲーム内シングルトン、アドオン、インベントリ、アクション呼び出しのNULL・状態確認を追加
- モジュールごとの更新・描画処理を例外分離し、1機能の失敗がプラグイン全体へ波及しないよう強化
- デミデジョン成功後、たんきゅうしんの対象バフが未付与または設定したしきい値以下なら再付与し、元のサポートジョブへ復帰
- メイン画面に自動操作モード、CE移動、FATE移動の切替と、現在アクティブな操作モード表示を追加

# 4.1.0

- 「たんきゅうしん」を有効にしている場合、4種類の30分バフのうち1つでも切れているか、残り時間が設定値以下になると確実に再付与
- 「たんきゅうしん」使用後に、変更前のサポートジョブへ復帰する処理を強化
- プレイヤーまたはvnavmeshが移動中の場合、デミデジョンを詠唱しない安全判定を追加
- 画面、設定、コマンド、内部クラス名を「自動操作モード」に統一
- 自動操作モード用コマンドを `/ciut auto`、`/ciutauto`、`/crescentauto` に変更

# 4.0.0

- ソースフォルダ、プロジェクト、アセンブリ、名前空間、内部識別子を `CrescentIsleUsefulTool` に統一
- 配布DLLとマニフェストを新しい内部識別子へ移行
- 旧来の開発用クラス名、キューID、翻訳表記、VS Codeビルドパスを製品名準拠へ整理
- 互換ライブラリをクリーンなローカルソースからビルドする構成へ変更
- 未使用だった外部データ送信モジュールと固定APIキーを削除

# 3.3.0

- マジックポットFATEの実測開始時刻を検知し、30分周期の次回発生予想へ反映
- 財宝誘導バフ、マジカルエリクサー、方向・距離ヒントを利用するマジックポット宝箱探索モードを追加
- 北征編マジックポット宝箱の候補座標を追加
- トレジャーハントを停止した地点から再開できるよう変更
- 取得済みの青銅・白銀宝箱数を追加し、複数の宝箱オブジェクトを個別に追跡
- FATE／CEが妖火の漁村エリア外にある場合、妖火の漁村エーテライトを移動候補から除外

# 0.11.0

- Updated UI to include both a teleport and move to button
- Can no longer click teleport if you are already next to the destination aetheryte
- Updated aethenet shard for Brain Drain
- Added some custom paths for certain fates, so that the path taken to walk to them is more natural

# 0.12.0

- Removed Crowdsourcing module
- Added WindowManager Module
    - This module allow you to configure if the main and config windows open and close on plugin load, enter zone and
      exit zone

# 0.12.1

- Changed labels in WindowManager config slightly
